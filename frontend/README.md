# News Analyzer Frontend

React + Vite + TypeScript + Tailwind CSS UI for the News Analyzer app.

## Setup

```bash
cd frontend
npm install
npm run dev
```

The dev server runs on `http://localhost:5173` and proxies `/api` calls to the backend at `http://localhost:8000`.

## Build

```bash
npm run build
npm run preview
```
